# Fill these values locally — DO NOT commit real credentials to version control
# We convert the provided SMTP_PASSWORD (SendGrid key) into SENDGRID_API_KEY and
# set SMTP relay fields for convenience. Keep this file local and do not commit.
# Fill these values locally — DO NOT commit real credentials to version control
# For Gmail SMTP testing, set these values. Replace the SMTP_PASSWORD value
# with the 16-character App Password you generate in your Google Account.
SMTP_HOST = "smtp.gmail.com"
SMTP_PORT = "465"
SMTP_USER = "michealchinedu209@gmail.com"
SMTP_PASSWORD = "kfltbdcyhqdbgqwr"
RECIPIENT_EMAIL = "f26202641@gmail.com"
